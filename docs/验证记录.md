# 验证记录(Phase 0-8 + 正式商品同步 + P0.1-P0.4)

> 每条记录均为当时实际运行结果;测试套件的最新结果必须以当次 `uv run pytest -q` 输出为准。

## Phase 0 — 脚手架

- `uv run xianyu-agent --version` → `xianyu-agent v0.1.0`
- Alembic 迁移建 11+1 张表,`data/verify_tables.py` 输出确认
- ruff 33 条规则全绿;pytest 冒烟 8 项通过

## Phase 1 — 协议层

- 19 个 parser/signer/events 单测 + mock WS 集成测试
- e2e:`auth login` → `protocol inject` → `message list/show` 全链路,含原始 payload 展示
- 修复:事务 bug、inject 卡队列、websockets 17 API、system 分支顺序

## Phase 2 — 账号池 + Worker

- 集成测试:mock WS 上 3 账号同时在线,心跳写库
- `scripts/demo_pool.py`:`pool start-all` 前台看板显示 3 账号 connected + 心跳时间
- 修复:`WorkerStatus.account_id` 数字 FK 与字符串匹配错位

## Phase 3 — 规则回复

- 集成测试:收买家消息 → 命中关键词规则 → mock server 实际收到回复 → `reply_logs success=True`
- `scripts/demo_reply.py` 全链路:rule add/list/test → inject → 2 条回复 + hit_count 落库

## Phase 4 — 自动发货 + 心跳清理

- 集成测试:OrderPaid → 原子扣卡 → 发送 → 订单 delivered + 消费记录 success
- 演示:高额订单被拦、发送失败如实记录、delivered 确认帧不掩盖失败
- 修复:SQLite 外键未启用、状态机防伪

## Phase 5 — 驾驶舱 TUI

- Pilot 测试:四区渲染账号/消息/卡密、紧急模式写审计、q 退出
- 真实终端启动渲染成功(订单表头 + 卡密数据行可见)

## Phase 6 — MCP + Skills

- API 端点测试(账号/认证/规则/卡密流)全 200
- `mcp serve` 实启:`/sse` 返回标准 MCP 握手,`pool/status` 正常 JSON
- 4 个 SKILL.md + `.claude-plugin/marketplace.json` 就绪

## Phase 7 — AI 回复 + guardrails

- 三模式引擎测试(rule / rule_then_ai / ai,未配置静默降级)
- guardrails 四闸单测 + 演示:999999 元订单被拦,审计事件可见;TUI 红色告警条测试

## Phase 8 — 文档 + 覆盖率

- 协议解析/规则匹配/卡密原子扣减/账号池启停/guardrails 五个关键模块覆盖率 **100%**
- 总覆盖率 **82.83%**(门槛 60%)
- 录制帧端到端:5 帧回放 → 消息/回复/发货/防伪全验证
- 收口:实现 `message send`、Windows 启动脚本、README/AGENTS 一致性

## 未完成(需真实凭据)

- WS 已由无时限 daemon 持有,账号级 `pool` 跨进程控制与 Windows 强杀恢复已完成;
  Windows 重启后的自动恢复尚未验证。
- 真实入站买家消息字段、真实文本发送 ACK、真实订单事件和发货闭环尚未校准。
- 第二个真实账号并行与 24 小时长稳尚未验证。

上述缺口按 `docs/开发计划.md` 的 P0-P6 顺序推进;当前先完成 P0 WS 常驻与 Windows 自恢复。

## 正式在售商品接入(2026-08-12)

- 清理运行库中的测试业务数据并回读确认:`cards`、`card_consumptions`、`orders`、`messages`、
  `reply_rules`、`reply_logs`、`audit_logs` 均为 **0**;账号和加密 Cookie 保留。
- 新增 `items` 本地只读镜像表(Alembic `ee4c2be8f4d1`)与 `item sync/list/show` CLI、
  FastAPI/MCP `item_sync/item_list/item_get` 工具。
- 实测:`xianyu-agent item sync --account xcaicai --page-size 20 --max-pages 20`
  成功同步 **10 条 / 1 页**在售商品;本地镜像中的在售条数为 **10**。
- 同步请求只使用闲鱼的商品列表接口;不执行上架、编辑、下架、删除、购买或发货。

## P0 WS 常驻开发门槛

P0 完成必须分别保留以下真实运行证据,不能用 mock 测试替代:

- 真实账号前台常驻 30 分钟。
- 断网 30 秒后自动重连。
- 强制结束 daemon 后由 Windows 自动拉起。**已于 2026-08-14 通过。**
- Windows 重启后无需打开终端即可恢复。
- 连续 24 小时无重复处理、未解释退出和敏感信息泄漏。

除强杀恢复外其余项目尚未完成,因此项目仍未达到“可持续无人值守运行”的完整验收状态。

## P0.1 daemon 核心(2026-08-13 至 2026-08-14)

- 新增 Alembic `4dc9fbc82a37`,正式数据库已迁移至 head;只新增 `daemon_instances` 表。
- 实现 `daemon run/status/stop/restart/logs`:无 `--seconds`、进程级 SQLite 控制、90 秒 stale
  判定、单实例文件锁、账号启用状态对账和优雅退出。
- 日志写入 `data/logs/xianyu-agent.log`,5 MiB 轮转、保留 5 个备份;常见 Cookie/Token、
  Fernet key、卡密字段和已知消息正文日志会在写入与 CLI 输出时脱敏。
- 静态检查:`uv run ruff check src tests` → `All checks passed!`。
- 全量测试:`uv run pytest -q` → **131 passed**。
- 真实短时冒烟:daemon 被跨进程查询为 online;`xcaicai` 为 connected;`daemon stop` 后
  子进程 PID 已不存在,DB 为 `stopped` 且无错误。
- 单实例真实冒烟:第二次 `daemon run` 返回“单实例锁已被占用”且退出码为 1。
- `demo` 账号启用但没有有效 Cookie,仅该 Worker 重连并写错误,未影响 `xcaicai`。

边界:本节只证明 P0.1 核心链路,不能替代 P0-A 至 P0-E 的 30 分钟、断网、杀进程、
Windows 重启和 24 小时验收。此处记录的是 P0.1 当时状态;后续强杀恢复见 P0.3。

## P0.2 账号级跨进程控制(2026-08-14)

- 新增 Alembic `9a7e615f2c41`:为 `accounts` 增加 `desired_state`,新增 `worker_commands` 表;
  临时 SQLite 升级→降级→再升级通过,正式数据库已迁移至 head。
- `pool start/stop/restart/start-all/stop-all` 改为持久化命令;daemon 原子领取 pending 命令,
  写入 running 与 succeeded/failed 终态。CLI 只有收到 succeeded 才显示 OK。
- 单测覆盖命令原子领取、终态、批量提交、禁用账号、安全默认 stopped、旧 daemon 遗留命令
  失败收口以及 daemon 离线返回非零退出码。
- 最终静态检查:`uv run ruff check src tests` 与冒烟脚本检查均通过;全量测试
  `uv run pytest -q` → **142 passed**。
- 真实账号连续验收:`stop → disconnected`、`start → connected`、`restart → connected`;
  三条记录分别为 `stop:succeeded:stopped`、`start:succeeded:started`、
  `restart:succeeded:restarted`,daemon 最终退出码 0。
- 可重复脚本:`uv run python scripts/smoke_worker_control.py --account xcaicai --timeout 30`。

边界:P0.2 不包含 Windows 自启动、进程崩溃拉起、TUI/MCP daemon 控制或 24 小时长稳。

## P0.3 Windows 启动与自恢复(2026-08-14)

- 实现 `service install/start/stop/status/uninstall`,以安装时解析的绝对 `pythonw.exe` 和工作
  目录创建隐藏的 `XianyuAgent-Daemon` 主任务。
- 初版只配置主任务 `RestartOnFailure`;真实强杀 PID 后任务停在 Ready 且结果为
  `0xFFFFFFFF`,100 秒内没有恢复。该失败被保留,未按通过处理。
- 根据失败证据增加 `XianyuAgent-Watchdog`:独立 TimeTrigger 每分钟运行,结合数据库状态、
  90 秒心跳和 PID 判断 daemon 健康,异常时重新运行主任务。
- 修复首次双任务并发时 Watchdog 误杀 `starting` daemon 的竞态;新鲜的 starting/stopping
  实例不再被终止,只有 stale 的存活实例才进入结束与重启路径。
- 增加 `data/runtime/service.paused` 人工暂停门;`service stop` 后等待 70 秒,
  `SERVICE_RUNNER_COUNT=0`,确认不会被 Watchdog 误拉起;随后 `service start` 恢复 online。
- P0-C 真实验收:强制结束旧 daemon PID `2620`;Watchdog 下一分钟执行成功,有效 daemon
  恢复为 PID `19816`,`xcaicai` 恢复 connected,总耗时小于 75 秒且只有一个有效 daemon。
- 当前环境采用 `--startup user`,两个任务均已安装;`demo` 因无正式 Cookie 已停用,
  `xcaicai` 保持 `enabled + desired_state=running`。
- 最终检查:`uv run ruff check .`、`git diff --check` 通过;
  `uv run pytest -q` → **163 passed**。

边界:P0-D Windows 重启尚未执行;P0-A、P0-B 与 P0-E 长稳验收也仍待完成。

## P0.4 健康检查与可观测性(2026-08-14)

- 新增统一只读运行快照:`daemon status`、`doctor` 与 TUI 共用 daemon 状态、PID、90 秒
  stale 阈值及账号 Worker 期望/实际判定,不再用界面进程内的空 `AccountPool` 伪造状态。
- `daemon status` 新增运行时长和账号表,展示 enabled、desired、actual、aligned、
  heartbeat age 与脱敏错误。
- 实现 `doctor --output table|json`:检查 SQLite 连接及 `BEGIN IMMEDIATE` 写锁、Alembic
  当前版本/head、Fernet 格式、Cookie 可解密性、日志目录、daemon 单实例/遗留记录、
  Worker 状态差异和 Windows 主任务/Watchdog。FAIL 返回 1,WARN 不阻断。
- 正式库真实结果:`PASS=8 WARN=1 FAIL=0`,表格与 JSON 退出码均为 0。WARN 仅为停用
  `demo` 保留不可解密旧 Cookie;正式账号 `xcaicai` Cookie 可解密,未输出任何凭据值。
- TUI 正式库 Pilot 冒烟:`daemon: online`、运行时长、账号 2、漂移 0、在售商品 10;
  `xcaicai=running/connected/Y`,`demo=stopped/disconnected/Y`。单测另覆盖 stale 与漂移红条。
- 最终检查:`uv run ruff check .` 通过;`uv run pytest -q` → **174 passed**。

边界:P0.4 是诊断与可观测性完成,不替代 P0.5 的断网、Windows 重启和 24 小时长稳证据。

## P0.5 / P0-A 真实前台常驻 30 分钟(2026-08-14)

- 新增可重复验收脚本 `scripts/smoke_daemon_stability.py`;脚本启动真正的前台
  `daemon run`,独立进程每 10 秒从 SQLite/PID 统一快照采样,结束时调用 `daemon stop`。
- 正式执行持续 **1800 秒**,写入 **181 个样本**;实例 ID 始终为
  `25c7a8590b4a4abc8a2abb2684a2fdf1`,PID 始终为 `34164`。
- 181 个样本全部满足 daemon=online、`xcaicai` actual=connected、aligned=true;
  最大 daemon 心跳年龄 **5.028 秒**,最大 Worker 心跳年龄 **29.117 秒**。
- `reconnect_attempts` 最大值为 **0**,`last_error` 非空样本为 **0**,没有实例或 PID 变化。
- 结束时 `ORDERLY_EXIT=PASS`,`DAEMON_EXIT_CODE=0`;随后恢复 Windows service,
  新 daemon PID `37352` online,`xcaicai` connected,doctor 为 8 PASS / 1 WARN / 0 FAIL。
- 原始 JSONL 位于 `data/logs/p0-a-daemon-stability.jsonl`,受 `.gitignore` 保护,不含 Cookie、
  Token 或消息正文;只保存实例、PID、状态、心跳年龄、重连次数和错误字段。

结论(2026-08-14 修订):该次 P0-A 只证明传输层长连接与 daemon 稳定。后续代码审计确认
当时客户端未发送 `/reg` 与 `ackDiff`,不能证明闲鱼消息订阅在线。保留原始证据,待订阅层
真实消息校准后重跑 P0-A。P0 尚未完成。

## P0.5 / P0-E 可恢复长稳监测器(2026-08-14)

- 实现 `soak start/status/stop`:运行与聚合指标持久化在 `task_logs`,Windows Watchdog 每分钟
  调用 `sample_active_soaks`;Codex/终端退出后仍可继续,重复启动会被拒绝。
- 每个脱敏样本记录 daemon 实例/PID/状态/心跳年龄、账号 desired/actual/aligned、Worker
  心跳年龄、重连次数和脱敏错误,不包含 Cookie、Token 或消息正文。
- 聚合门覆盖:≥90% 采样、最大采样间隔、异常/stale/故障窗口、daemon 实例变化、意外启动、
  入站/出站消息、成功/失败回复、同一消息重复成功回复及敏感日志值。
- 敏感扫描使用本地真实 Fernet key、加密 Cookie、解密 Cookie 及各 Cookie 字段值;按文件偏移
  增量扫描并覆盖日志轮转变小场景。扫描测试已验证新泄漏被命中、旧内容不重复计数。
- 36 秒真实短跑自动完成为 success:3 个健康样本,daemon 实例变化 0、重复回复组 0、敏感
  日志命中 0。该结果只证明监测器工作,不计作正式 P0-E 通过。
- 最终检查:`uv run ruff check .` 通过;`uv run pytest -q` → **186 passed**。

下一步:在正式账号在线且 Windows 服务健康时启动 24 小时运行;到期前不得标记 P0-E 通过。

### P0-E 首次正式运行已停止(口径无效)

- 2026-08-14 03:38:18 启动 `run_id=466dd46e277a4ec9ba79408a338b496d`,账号
  `xcaicai`,目标 86400 秒,截止 2026-08-15 03:38:18(Asia/Shanghai)。
- 初始 daemon 实例 `08eed53279bc45ba9ddd178ae23dce27`,PID `37352`;启动与手工核验
  共 2 个健康样本。
- 03:39:05 Windows Watchdog 自动执行,03:39:08 样本自动增至 3;任务结果 0、下次运行时间
  正常,证明采样不依赖当前终端或 Codex 会话。
- 当前 3/3 样本健康,stale、故障窗口、重连、实例变化、重复回复、失败回复和敏感日志命中
  均为 0。原始证据:`data/logs/p0-e-soak-466dd46e277a4ec9ba79408a338b496d.jsonl`。

状态:**skipped,未通过。** 代码审计确认当时的 `connected` 在 `websockets.connect()` 后立即
写入,客户端没有发送 `/reg`、`/r/SyncStatus/ackDiff`,也没有 ACK/解包
`syncPushPackage`。因此 29 个健康样本只代表传输层握手与进程心跳,不能代表闲鱼消息订阅。
该 run 已主动停止,关联每小时核验任务已删除;证据保留,不得删除或改写为成功。

## P0 订阅初始化修正(2026-08-14)

- 新增 IM token provider:使用已加密 Cookie 调用
  `mtop.taobao.idlemessage.pc.login.token/1.0/`,accessToken 以 Fernet 密文写入
  `ws_credentials`,设备 ID 稳定复用。
- WS 状态顺序改为:获取凭据 → transport connect → `/reg` → `ackDiff` → connected。
- 服务端推送先返回 code=200 ACK,再解包 `syncPushPackage.data[*].data`;支持 Base64 JSON
  和 Apache-2.0 `msgpack` 库,未复制 GoofishEngine 的 AGPL 解码实现。
- 标准化字段新增 `item_id`、`sent_at`;消息唯一键改为 `(account_id,message_id)`;
  系统 `MsgTips` 独立写审计日志,不进入买家消息自动化。
- 自动化默认改为 `XIANYU_AUTOMATION_MODE=observe`,真实校准只落库;只有显式 `active`
  才启用规则回复和自动发货。
- 正式数据库副本的 Alembic `upgrade head` 与 `downgrade 9a7e615f2c41` 均通过。
- 最终离线检查:`uv run pytest -q` → **197 passed**;`uv run ruff check .` 通过。

下一步:迁移正式库并以 `observe` 启动真实账号;确认 token、注册与同步成功后,由测试买家
发送唯一文本完成字段校准,再重跑 P0-A/P0-C/P0-E。

### 正式账号首次订阅联调

- 正式库已备份后迁移到 `c55a71e3a2b4`;daemon 以 `observe` 启动,PID `47708`。
- 客户端真实调用 IM token 接口,HTTP 200,业务返回
  `FAIL_SYS_SESSION_EXPIRED::Session过期`;因此状态正确停留在 error/reconnecting,没有伪报
  connected,也没有进入 `/reg` 步骤。
- 已通过账号池命令停止 `xcaicai` Worker,避免重复请求触发风控;daemon 保留在线。
- 补充认证失败策略:首次 WsAuthError 即进入 error,冷却 300 秒再尝试;`httpx/httpcore`
  持久日志级别降为 WARNING,避免 mtop sign/timestamp 查询串写入 INFO 日志。

结论:真实订阅测试当前被已保存 Cookie 的 Session 过期阻断。需重新执行扫码登录后再启动
`xcaicai` Worker;未验证前不得宣称消息订阅成功。

### P1 校准工具前置补强

- 新 Cookie 保存会让旧 IM token 立即过期,但保留原 device ID;扫码流程会汇总重定向和
  各步骤响应 Cookie,避免只保存最终响应的残缺会话。
- 新增账号级跨进程连接锁,daemon Worker、`protocol connect/capture`、手工登录、扫码登录
  与 token refresh 不能同时占用同一账号。
- `auth refresh` 已由占位改为真实 IM token 换取;`auth status` 只输出 unb 掩码、mtop token
  摘要、IM token 是否有效、设备摘要和过期时间。
- 新增 `protocol capture`:只运行观察模式,收到标准事件后落库,不会实例化回复/发货服务;
  JSONL 保留协议字段结构,所有非协议字符串、URL 值和嵌套 JSON 值不可逆脱敏。
- 常驻 Worker 的业务 `raw_payload` 同样改为脱敏结构,不再把原始买家/会话字段明文写入库。
- capture 支持 `--target-messages`,达到目标买家消息数后提前退出并写 summary;摘要列出连接、
  帧/事件/消息/系统通知数量、重复消息事件和缺失标准字段。所有值摘要使用每次运行随机盐
  HMAC-SHA256,盐不落盘,降低已知短句/昵称被离线枚举反查的风险。
- 真实失败路径复测:Session 过期时 capture 立即显示 `ws_auth`、汇总 errors=1、退出码 2;
  无数据库连接取消堆栈;daemon 继续 online,账号保持 stopped/disconnected/aligned。
- `auth status` 运行态只显示 `unb_masked`、`m_h5_tk_digest`、`device#摘要` 和有效性,
  不显示完整身份或凭据。
- 本批全量检查:`uv run pytest -q` → **205 passed**;`uv run ruff check .` 与
  `git diff --check` 通过。
- 真实重新扫码首次尝试在 `passport mini_login` 阶段遇到异步 TLS `ConnectError`;同步可达性
  探测为 HTTP 200,确认是传输抖动而非固定接口失效。扫码三阶段和状态轮询已增加有限传输
  重试,耗尽后显示阶段、异常类型与重试次数,不再输出空错误。
- 本机异步 TLS 还出现超时未按预期中断;现已为每次请求增加 `asyncio` 硬超时,异步重试
  耗尽后回退到有界同步 HTTP。二维码文件改为先保存 PNG,再打印仅 ASCII 的终端预览,
  避免 Windows GBK 无法编码 NBSP 导致“内容已生成但 PNG 未保存”。
- 真实复测成功生成 `data/qr_logins/9abeaa27...png` 并输出 ASCII 预览;会话等待 300 秒后
  因未扫码正常过期。全量检查更新为 **208 passed**,ruff 与 `git diff --check` 通过。

### 注册确认与重连重放补强

- GoofishEngine 新 IM 客户端的只读审计显示:`/reg` 使用请求 mid 等待服务端匹配响应,
  再 ACK 并发送 `ackDiff`;本项目已按该确认顺序自写实现,不再用固定 sleep 充当注册成功。
- 注册响应缺省 code 按 200,明确非 200 拒绝;5 秒超时也不进入 connected。
- 本地 WS 集成测试覆盖注册请求 → 匹配响应 → ACK → ackDiff 顺序,以及多账号池、规则回复、
  发货链路;定向结果 **39 passed**。
- 新增断线重连重放集成测试:服务器每次连接推送相同 `message_id`,客户端至少连接两次,
  SQLite 最终只有一条消息;定向结果与注册/解析测试合计 **25 passed**。
- 新增 `protocol capture-verify`:离线检查唯一 summary、connected、目标消息、必需字段、
  重复事件、捕获错误及常见敏感字段明文;验收失败返回非零,避免人工目测 JSONL。
- 本批最终全量检查:`uv run pytest -q` → **211 passed**;ruff 与 `git diff --check` 通过。

### 扫码闭环与捕获证据一致性校验

- `auth qr-login` 在 Cookie 加密保存成功后会立即强制换取 IM accessToken 并加密缓存;
  换取失败保留 Cookie,但命令返回退出码 3 并提示使用 `auth refresh` 重试,不再把“只拿到
  Cookie”误报为完整 WS 登录就绪。
- `protocol capture` 现在把连接状态和错误写入同一份不可逆脱敏 JSONL;状态详情和错误消息
  不保留原文。
- `protocol capture-verify` 会将 summary 的 frames/events/messages/system_notices/errors/
  connected 与实际记录交叉核对,并扫描 `payload`、`decoded_sync`、state/error 中所有字符串;
  数字协议键(例如 `body.1`)下夹带明文也会使验收失败。
- 新增扫码 Cookie→IM token 单步持久化测试、connected 实证测试、summary 统计/目标一致性、
  未知字段与数字键明文拒绝测试。本批全量检查:`uv run pytest -q` → **215 passed**;`uv run ruff check .` 与
  `git diff --check` 通过。
- 2026-08-15 已重新扫码;`auth status` 显示 Cookie、`_m_h5_tk`、`cookie2` 和 IM token 均有效。
  Worker 仍由此前的 `pool stop` 保持 `desired=stopped/actual=disconnected/aligned=Y`;
  尚未启动，因此真实 `/reg` 确认和首条买家消息捕获仍未动态验证。

### 驾驶舱在售商品明细

- 驾驶舱从四区扩展为五区;在售商品面板横跨底部整行，读取本地在售商品镜像并显示账号、标题、
  价格、闲鱼返回状态、商品 ID 与最后同步时间。
- 顶部“在售商品”数量复用商品面板加载结果，不再额外进行一轮相同的 SQLite 查询。
- Textual Pilot 覆盖商品明细行和既有驾驶舱状态栏;定向结果 `4 passed`。本批全量检查:
  `uv run pytest -q` → **215 passed**;`uv run ruff check .` 与 `git diff --check` 通过。

### 卖家已售订单只读镜像与商品已售汇总(2026-08-21)

- 新增迁移 `f1a55ff834d2`:为 `orders` 增加 `quantity`、`placed_at` 和
  `(account_id,item_id,status)` 查询索引。正式 SQLite 已从 `c55a71e3a2b4` 成功升级至该 head;
  临时空 SQLite 已完整跑通全部迁移链。
- 新增 `order sync --account <id>`:候选卖家订单接口只读分页、每页间隔 1 秒、最多由
  `--max-pages` 限制。接口失败时先抛错，领域入库不会执行，因此不会改写已有订单镜像。
- 订单响应只白名单保留订单/商品/状态/时间、买家 ID/昵称、金额和数量；电话、收货地址等
  字段不写入 `orders.raw_payload`。
- 商品明细已移除闲鱼原始 `状态` 列，改为“已售”。口径为同账号同商品下 `paid`、`delivered`、
  `completed` 订单的 `quantity` 合计；退款、取消、未知状态均不计入。
- 离线验证:`tests/unit/test_orders_sync.py` + `tests/unit/test_tui.py` → **8 passed**;
  `ruff check src/xianyu_agent tests/unit/test_orders_sync.py tests/unit/test_tui.py` → 通过。
- 首次真实只读同步:`order sync --account xcaicai --page-size 30 --max-pages 100` 返回
  “Cookie 已失效，请重新扫码登录”。没有成功响应、没有订单写入、没有发货/消息/评价等远端动作。
  因此订单 API 的实际响应路径和字段仍为**待真实校准**，不得将本节当成历史订单同步通过证据。

后续复测:重新扫码登录后，执行
`uv run xianyu-agent order sync --account xcaicai --page-size 30 --max-pages 100`，再执行
`uv run xianyu-agent item list --account xcaicai` 和 `uv run xianyu-agent dashboard` 核验已售列。

### 卖家已售订单真实校准通过(2026-08-21)

- 重新扫码后，IM Token 端点仍返回 `FAIL_SYS_USER_VALIDATE`，因此 Worker 保持停止；这不影响
  独立的卖家订单读取验证。
- 真实命令 `order sync --account xcaicai --page-size 30 --max-pages 100` 成功：远端报告
  **44** 条、共 **2** 页，本地解析 **44** 条，新增 0、更新 44。该结果确认订单接口、分页、
  订单 ID、商品 ID、状态、金额和数量读取链路可用。
- 同步后的本地只读汇总：订单状态为 completed=33、delivered=1、cancelled=10；按“已付款/
  已发货/已完成”且求 `quantity` 的口径，共 **34** 件已售，覆盖 **7** 个商品。
- 当前在售商品镜像共有 11 条；`item list` 已能展示每条商品的“已售”数量。Worker 未启动，
  没有发生消息发送、发货、退款或评价动作。
